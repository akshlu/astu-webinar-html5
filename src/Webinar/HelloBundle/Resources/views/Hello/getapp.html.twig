{% extends '::base.html.twig' %}
{% block body %}
    {% for app in apps %}
        <h2>{{app.getName()}}</h2>
        <p>
            {{app.getIdApplication()}}
            {{app.getAlias()}}
        </p>
    {%endfor%}
{% endblock %}